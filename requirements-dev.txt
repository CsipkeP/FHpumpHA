# Development and test dependencies.
# pymodbus is also the integration's only runtime requirement; keep the range in
# sync with manifest.json and with whatever Home Assistant core ships.
pymodbus>=3.6.9,<4.0.0
pytest>=8.0
pytest-asyncio>=0.23

# Home Assistant test harness.  It pins its own Home Assistant version, which is
# what the integration is developed against.  Home Assistant does not support
# Windows; tests/win_compat.py patches around the two places that matter.
pytest-homeassistant-custom-component

ruff
