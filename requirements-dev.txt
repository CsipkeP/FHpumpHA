# Development and test dependencies.
# pymodbus is also the integration's only runtime requirement; keep the range in
# sync with manifest.json and with whatever Home Assistant core ships.
pymodbus>=3.6.9,<4.0.0
pytest>=8.0
pytest-asyncio>=0.23

# Home Assistant test harness.  Pinned, because it pins Home Assistant in turn:
# an unpinned upgrade silently moves the whole test environment underneath the
# suite.  Bumping it is a deliberate act, and worth doing regularly -- releases
# from 0.13.348 on require Python 3.14, which is what current Home Assistant
# runs on.
pytest-homeassistant-custom-component==0.13.316

ruff
