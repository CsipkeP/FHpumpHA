# Development and test dependencies.
# pymodbus is also the integration's only runtime requirement; keep the range in
# sync with manifest.json and with whatever Home Assistant core ships.
pymodbus>=3.6.9,<4.0.0
pytest>=8.0
pytest-asyncio>=0.23
